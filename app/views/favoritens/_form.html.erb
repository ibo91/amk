<%= form_for(@favoriten) do |f| %>
  <% if @favoriten.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@favoriten.errors.count, "error") %> prohibited this favoriten from being saved:</h2>

      <ul>
      <% @favoriten.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.text_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :karteikarten_id %><br>
    <%= f.text_field :karteikarten_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
