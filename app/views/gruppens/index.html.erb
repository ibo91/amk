<%- model_class = Gruppen -%>
<div class="page-header">
  <h1><%=t '.title', :default => Gruppen %></h1>
</div>
<table class="table table-striped">
  <thead>
    <tr>
      <th>ID</th>
      <th><%= model_class.human_attribute_name(:gruppenname) %></th>
      <th><%= model_class.human_attribute_name(:created_at) %></th>
      <th><%=t '.actions', :default => t("helpers.actions") %></th>
    </tr>
  </thead>
  <tbody>
    <% @gruppens.each do |gruppen| %>
      <tr>
        <td><%= gruppen.id %></td>
        <td><%= gruppen.gruppenname %></td>
        <td><%=l gruppen.created_at, format: :long %></td>
        <td>
          <%= link_to gruppen_path(gruppen), :class => 'btn btn-xs', :title => "#{ t('.show', :default => t('helpers.links.show')) }" do %>
            <%= glyph 'info-sign' %>
            <%= glyph 'pencil' %>
          <%- end -%>
          <%= link_to gruppen_path(gruppen), :method => :delete, :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) }, :class => 'btn btn-xs', :title => "#{ t('.destroy', :default => t('helpers.links.destroy')) }" do %>
            <%= glyph 'remove' %>
          <%- end -%>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to t('.new', :default => t("helpers.links.new")),
            new_gruppen_path,
            :class => 'btn btn-primary' %>
